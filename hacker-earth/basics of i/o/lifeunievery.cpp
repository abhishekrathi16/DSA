#include<iostream>
#include<bits/stdc++.h>
using namespace std;

int main(){
    int n; //to enter a number of one or 2 digits
    while(1)  //infinite loop
    {
        cin>>n;
        if(n!=42) cout<<n<<endl;
        else exit(0);
    }
    return 0;
}